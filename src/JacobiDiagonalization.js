const { SymmetricMatrix } = require('./Matrix');

class JacobiDiagonalization {
  constructor (symmetricMatrix, stepsAmount) {
    this.matrix = symmetricMatrix;
    this.steps = stepsAmount;
  }

  maxStrategy(){
    let previousMatrix = this.matrix;
    for (let i = 0; i < this.steps - 1; ++i) {

    }
  }
}
